# dogfooding-3-834 正式作业执行计划

## 1. 题目基础信息

- Dogfood ID：`dogfooding-3-834`
- 当前状态：待提交
- L1：`L1-UND`
- L2：`L2-02`
- 题型理解：`Web 前端代码理解与分析`
- 评测模型：`Doubao-Seed-Code-Dogfood-2.1.2`、`GLM-5.0`、`Kimi K2.5`

## 2. 执行总原则

- 先读 [正式作业执行指南_v1.6_最终执行版.md](/d:/project/dogfooding-3-834/正式作业执行指南_v1.6_最终执行版.md)，后执行。
- `Dockerfile` 必须在正式跑模型之前准备好，不能等模型输出后再反推环境。
- 正式开发与留痕统一落在**自己的 GitHub 仓库**中，不能直接在公域仓库上开发或提 PR。
- 正式题目必须能对应到 `Repo 链接 / Repo 介绍 / 技术栈 / Dockerfile / PR 链接` 这几类前置材料。
- 所有评分固定为 5 个维度，每个维度 1 到 5 分，总分 25 分。
- 每个模型最多 3 轮。
- 第 1 轮下发完整任务；第 2 轮和第 3 轮只能修复、补漏，不能新增需求。
- 每个模型必须新开独立对话，不能复用上下文。
- Trea 固定使用 `Builder` 模式，环境固定为 `ppe`，不能使用 `MCP`。
- 最终目标不是“只看模型回答”，而是“完成留痕、评分、问题分析、表格录入”。

## 3. 角色分工

- `Claude Code`：负责制作首轮 prompt，并在必要时制作第 2 轮、第 3 轮补充 prompt。
- `Claude Code`：也负责协助补齐题目材料，例如 `instruction.md`、`environment/Dockerfile`、`repo 说明`、`L3 技术栈判断`。
- `Trea`：作为正式评测平台，分别测试 `seed-doubao`、`Glm-5`、`Kimi-k2.5` 三个模型。
- `Codex`：负责解读执行指南、校验流程、判断是否需要第 2 轮/第 3 轮、整理评分口径、指导填写飞书表格。
- 你：负责在 Trea 中实际发起对话、复制 session id、粘贴模型原始输出、在自己的 GitHub 仓库中保留 repo 与 PR 留痕、执行最终提交。

## 4. 标准执行步骤

### 第一步：准备环境

1. 打开 Trea CN。
2. 确认当前模式为 `Builder`。
3. 在设置中确认请求环境是 `ppe`。
4. 确认模型列表中可以切换到：
   - `Doubao-Seed-Code-Dogfood-2.1.2`
   - `GLM-5.0`
   - `Kimi K2.5`
5. 确认本地目录结构已建立：
   - [environment](/d:/project/dogfooding-3-834/environment)
   - [seed-doubao](/d:/project/dogfooding-3-834/seed-doubao)
   - [Glm-5](/d:/project/dogfooding-3-834/Glm-5)
   - [Kimi-k2.5](/d:/project/dogfooding-3-834/Kimi-k2.5)
6. 正式开做前先判断题目属于哪一种：
   - 基于现有 repo 改造 / 分析
   - 从 0 到 1 自建题目
7. 判断完成后，再决定 GitHub 和 repo 的准备方式，不能跳过这一步。

### 第二步：准备 GitHub、Repo 和题目基础材料

1. 正式作业统一使用**自己的 GitHub 仓库**。
2. 如果题目基于现有开源 repo：
   - 先同步到你自己的 GitHub 仓库
   - 后续分支、开发、PR 都在你自己的仓库里进行
   - 不能直接在原始公域仓库上建分支和提 PR
3. 如果题目是**我们自己创造的**：
   - 代码和环境也要由我们自己准备
   - 可以自建公开 GitHub repo
   - 这个 repo 要承载题目的 base 环境或初始代码现场
4. GitHub 什么时候使用：
   - 在正式放 prompt 到 Trea 测试前就要使用
   - 在确定题目类型和 base 环境后就要准备 repo
   - 在需要提交表格中的 `Repo 链接`、`PR 链接` 时必须已经有对应留痕
5. Prompt 放到 Trea 测试时，GitHub 是否要先有代码，分两种情况：
   - 非 0 到 1 题目：要，GitHub 中应已有需求前的初始代码
   - 从 0 到 1 题目：可以没有业务代码成品，但至少要先有 repo 现场、`git init` 记录和提前准备好的 `Dockerfile`
6. 自建题目的代码是否需要我们自己创造：
   - 需要
   - 如果是从 0 到 1 题目，我们至少要自己先创造题目的 base 环境
   - 如果不是纯生成题，还需要自己准备让模型分析或改造的初始代码现场
7. 题目基础材料必须至少包括：
   - Prompt
   - Repo 链接
   - Repo 介绍
   - Repo 使用的技术栈
   - 初始环境 Dockerfile
   - PR 链接（如适用）
8. 如果这些材料缺失，不允许直接跳进三个模型正式测试。

### 第三步：先写并验证 Dockerfile

1. 在正式做题前，先确认题目的 base 环境。
2. 先建立统一交付目录：
   - [environment](/d:/project/dogfooding-3-834/environment)
   - [environment/repo](/d:/project/dogfooding-3-834/environment/repo)
3. `Dockerfile` 必须面向 base 初始环境，而不是模型做完后的环境。
4. 三个模型共用同一份 base `Dockerfile`。
5. 编写时必须严格遵守这些规则：
   - 工作目录固定为 `/app`
   - `Dockerfile` 要和 prompt 中声明的语言、版本、框架、包管理器保持一致
   - 如果是非 0 到 1 题目，`environment/repo` 必须是需求前的初始现场
   - `repo/` 自身必须是 `git init` 过的现场
   - 从 0 到 1 题目时，`environment/repo` 可以是已 `git init` 的空目录
   - `dogfooding.json` 不得进入最终提交
   - 不允许先跑模型再倒推依赖环境
6. 编写完成后必须做验证：
   - 能 `docker build`
   - 容器能启动
   - 项目能在 base 环境正常运行
7. 只有 `Dockerfile` 通过验证后，才能进入下一步制作 prompt。

### 第四步：制作首轮 Prompt

1. 由 `Claude Code` 根据本题的 `L1-UND + L2-02` 生成正式 prompt。
2. prompt 必须满足：
   - 明确这是代码理解与分析任务，不是改代码任务。
   - 明确要求基于真实代码进行分析，不能臆测。
   - 明确要求输出结构化分析结果。
   - 明确要求给出具体文件、代码片段、优先级和改进建议。
3. 生成后把最终版 prompt 保存到：
   - [seed-doubao/记录模板.md](/d:/project/dogfooding-3-834/seed-doubao/记录模板.md)
   - [Glm-5/记录模板.md](/d:/project/dogfooding-3-834/Glm-5/记录模板.md)
   - [Kimi-k2.5/记录模板.md](/d:/project/dogfooding-3-834/Kimi-k2.5/记录模板.md)
   里的 `首轮 Prompt` 区域，保持三边一致。
4. prompt 中还必须显式写出目标环境，使之与 `Dockerfile` 完全一致。
5. prompt 能否进入 Trea 测试的前提检查：
   - GitHub repo 已准备
   - 题目类型已判断清楚
   - `Repo 链接 / Repo 介绍 / 技术栈 / Dockerfile` 已能落表
   - `environment/repo` 已是模型真正看到的初始状态
6. 正式提示词应尽量贴近真实用户表达，避免明显 AI 痕迹。

### 第五步：在 Trea 分别测试三个模型

1. 先测 `seed-doubao`。
2. 新开独立会话，粘贴首轮 prompt。
3. 等模型完整输出后，记录：
   - session id
   - 对话轮次
   - 模型原始回答
   - 是否完成任务
   - 是否出现问题
4. 再分别对 `Glm-5`、`Kimi-k2.5` 重复完全相同步骤。
5. 严禁一个模型的结果代替另一个模型。

### 第六步：由 Codex 判断是否进入第 2 轮或第 3 轮

你每测完一个模型，就把下面内容发给我：

1. 模型名称
2. session id
3. 该轮完整 prompt
4. 模型完整输出
5. 你观察到的问题

然后我按下面规则给你判断：

- 如果第 1 轮已经满足要求，没有明显问题，就停止，不进入下一轮。
- 如果有明显遗漏、理解偏差、结构不完整、没有按要求分析真实代码，就进入第 2 轮。
- 第 2 轮只能修复已有问题，不能新增要求。
- 第 2 轮仍未完成，才允许进入第 3 轮。
- 第 3 轮结束后必须停止，无论结果好坏。

### 第七步：评分与问题分析

每个模型都必须做五维评分：

1. 用户体验度
2. 任务拆分及执行
3. 理解/推理能力
4. 复杂指令遵循
5. 工程完备度

同时必须记录：

- 总分（25 分制）
- 问题类型
- 问题描述
- 修复成本
- 是否进入第 2 轮 / 第 3 轮

强约束：

- 开启第 2 轮后，理解/推理能力不能打到 4 分或 5 分。
- 工程完备度不是满分时，问题类型里必须包含“未闭环测试”。
- 指令遵循不是满分时，问题类型里必须包含“指令遵循失败”。
- 如果给 5 分，必须明确写出优点。
- 如果不给 5 分，必须明确写出问题和原因。

### 第八步：飞书表格填写

飞书链接：

- <https://bcnrsnl3m9wk.feishu.cn/app/Svtxbcihka3niSs3nr6ciMtUnhg?pageId=pgei0xJqPpfmC8Xp>

填写方式：

1. 你把表格中每个字段名称发我，或者截图给我。
2. 我根据每个模型的测试结果，逐项生成可直接粘贴的填写内容。
3. 你把我给你的内容粘贴到飞书。
4. 如果表里有字段不好判断，我会按指南口径帮你统一措辞。
5. 与 GitHub 相关的字段必须提前准备好：
   - Repo 链接
   - PR 链接
   - Repo 介绍
   - 技术栈
6. 如果是自建题目，repo 介绍中要明确说明这是自建项目或自建 base 环境，不要写成来源不明。

### 第九步：最终复核

提交前逐项检查：

- 是否已明确题目属于“现有 repo”还是“从 0 到 1 自建题目”。
- GitHub repo 是否已准备，且是你自己的仓库。
- 如果需要 PR，是否已在自己的仓库中留痕。
- `Dockerfile` 是否已编写并验证通过。
- `environment/repo` 是否是正确的初始现场。
- `repo/` 是否已经 `git init`。
- 三个模型是否都已独立测试。
- 每个模型是否都有 session id。
- 每个模型是否都记录了轮次。
- 每个模型是否都完成了五维评分。
- 问题类型、问题描述、修复成本是否齐全。
- 表格内容是否和实际测试记录一致。
- 评分是否符合指南，不出现 4 维、20 分制等旧口径错误。

## 5. 我们接下来的协作方式

你每次只需要把一个模型的一轮结果发给我，我会按下面顺序继续帮你：

1. 判断这一轮是否过关。
2. 如果不过关，我直接给你第 2 轮或第 3 轮 prompt。
3. 如果过关，我直接给你该模型的五维评分建议。
4. 三个模型都结束后，我统一帮你整理飞书表格填写稿。

如果前置材料还没齐，我也会先拦住，不让你直接去跑模型。优先补齐：

1. GitHub repo
2. `environment/repo`
3. `Dockerfile`
4. `Repo 链接 / Repo 介绍 / 技术栈`
5. 首轮 prompt

## 6. 当前已完成事项

- 已读取执行指南并提炼核心约束。
- 已建立三个模型目录。
- 已生成本题正式执行计划。
- 已补充 `Dockerfile` 的起始时机和单独执行阶段。
- 已补充 GitHub、Repo、自建题目与前置材料要求。

## 7. 下一步

下一步直接开始：先补齐 `environment/` 目录并准备 base `Dockerfile`，确认环境后再由 `Claude Code` 制作首轮 prompt。
